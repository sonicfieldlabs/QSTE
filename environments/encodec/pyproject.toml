[project]
name = "qste-encodec-environment"
version = "0.0.0"
description = "Separately locked, optional EnCodec adapter environment for QSTE P9"
requires-python = ">=3.10,<3.12"
dependencies = [
  "encodec==0.1.1",
  "einops==0.8.1",
  "numpy==1.26.4",
  "torch==2.11.0",
  "torchaudio==2.11.0",
]

[tool.uv]
package = false
