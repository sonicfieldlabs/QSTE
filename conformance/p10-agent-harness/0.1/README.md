# P10 agent-harness conformance

This profile verifies the bounded QSTE host and synthetic comparative controls.
It does not run a language model, establish agentic hearing, attribute
experience or creativity, or report a human/empirical study outcome.

Run `uv run python tools/verify_agent_harness.py`. The verifier requires no
network, model, playback, participant data, or external executor.
