# P5 deterministic STFT/Gabor conformance 0.1

This additive behavioral profile verifies `qste-stft-gabor/v0.1` without
changing the normative `qste-contract/0.3.0` schema set.

Run `make representation-contracts`. A pass establishes only the declared
deterministic representation capabilities. It does not establish DSQ
qualification, cross-representation relations, perception, or physical
calibration.
