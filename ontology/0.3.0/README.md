# QSTE ontology package 0.3.0

`QSTE_ontology.md` is a byte-identical snapshot of the approved
`qste-contract/0.3.0` source. Its SHA-256 is:

`02bce3f769a5d9e0d0ad9a4ff6664f6c2473ad5b3c8908a19fcfcee8e26ac2bf`

Non-public governance and sequencing material is intentionally absent from the
repository projection. The paper v3 link remains unavailable because its bytes
are withheld pending redistribution confirmation; the authority manifest
resolves its source ID, digest, availability, and reason.

Do not edit the contract snapshot in place. A semantic change requires an RFC
and a new contract version. A packaging-only correction still requires a
recorded successor digest rather than silent byte drift.
